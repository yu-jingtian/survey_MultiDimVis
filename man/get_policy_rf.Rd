% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_data.R
\name{get_policy_rf}
\alias{get_policy_rf}
\title{Get RF-predicted policy scores (2014–2021)}
\usage{
get_policy_rf(year = NULL, cols = NULL)
}
\arguments{
\item{year}{Integer vector of survey years to keep. Default \code{NULL} keeps all years (2014--2021).}

\item{cols}{Character vector of column names to return. Default \code{NULL} returns all columns.
Note: \code{case_id} and \code{year} are always included to support joins.}
}
\value{
A data frame containing RF-predicted policy scores.
}
\description{
Loads and returns the random-forest predicted policy score table, optionally
filtered by year and reduced to selected columns.
}
\examples{
df <- get_policy_rf()
df21 <- get_policy_rf(year = 2021, cols = c("immig_rf", "guns_rf"))
}
